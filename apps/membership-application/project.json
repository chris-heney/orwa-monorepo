{
  "name": "membership-application",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/membership-application/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project membership-application --web",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/membership-application"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "membership-application:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "membership-application:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "membership-application:build:production",
          "hmr": false
        }
      },
      "dependsOn": [{"projects": ["mm-strapi"], "target": "serve"}]
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/apps/membership-application"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "docker:build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker buildx build --platform linux/amd64 --no-cache --target production -t orwa/membership-application:latest -f Dockerfile.membership-application .",
        "cwd": "{workspaceRoot}"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "dependsOn": ["docker:build"],
      "options": {
        "command": "docker push orwa/membership-application:latest"
      }
    }
  }
}