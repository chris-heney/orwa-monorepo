import * as React from 'react';
import { ButtonProps } from '@mui/material';
export declare const PreviousButton: ({ alwaysEnable, disabled: disabledProp, ...rest }: PreviousButtonProps) => React.JSX.Element;
export interface PreviousButtonProps extends ButtonProps {
    alwaysEnable?: boolean;
}
//# sourceMappingURL=PreviousButton.d.ts.map