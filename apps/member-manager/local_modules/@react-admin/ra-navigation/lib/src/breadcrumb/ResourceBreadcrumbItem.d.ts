import { ReactElement } from 'react';
export type ResourceBreadcrumbItemProps = {
    resource: string;
    path?: string;
};
/**
 * The <ResourceBreadcrumbItem /> component allows to render a <BreadcrumbItem /> for a given resource.
 *
 * Also exported as `Breadcrumb.ResourceItem` for convenience.
 *
 * @see BreadcrumbItem
 */
export declare const ResourceBreadcrumbItem: ({ resource, path, }: ResourceBreadcrumbItemProps) => ReactElement;
//# sourceMappingURL=ResourceBreadcrumbItem.d.ts.map