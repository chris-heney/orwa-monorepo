/**
 * @deprecated Replaced by the framework page in `manifest.tsx`. Kept only so
 * the `modules/dashboards.ts` re-export still resolves; delete both together.
 */
export { default } from "./Conference";
