/**
 * Conference Command Center reuses the Grant Manager "water ledger" token set
 * so every modernized member-manager reporting surface shares one visual
 * language across light and dark mode.
 */
export {
  DARK,
  LIGHT,
  useSummaryTokens,
  display,
  money,
  type SummaryTokens,
} from "../../../grant-manager/grants/components/summary/tokens";
