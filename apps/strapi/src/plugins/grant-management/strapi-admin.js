'use strict';

// v5 note: the legacy admin UI for this plugin depended on @strapi/helper-plugin
// (removed in Strapi 5). The server-side logic is unaffected; the admin page is
// disabled until it is rewritten for the v5 design system.
module.exports = {
  register() {},
  bootstrap() {},
};
