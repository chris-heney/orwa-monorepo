# Copy to .env and fill in your values.
# NOTE: the endpoint must include /api — data/API.ts appends the resource
# directly (`${VITE_API_ENDPOINT}/watersystems`), it does not add the prefix.
VITE_API_ENDPOINT=http://localhost:13370/api
VITE_API_KEY=
